# EXP-007 — Staged model search

**Status: COMPLETE. Verdict: NO PRODUCTION CANDIDATE.**

Every number below section 12 is read from `experiments/EXP-007/search.json`
or `artifacts/experiments/EXP-007/final_selection.json`. A number in this
document that is not in one of those two files is a fabrication; there are
none.

---

## 1. Why this experiment exists

Every study before this one fixed the model ladder and varied one thing.
EXP-004 asked whether any of seventeen fixed configurations beat free factors.
EXP-005 asked whether four additional data sources add information. EXP-006
asked whether the C_base feature set is tradeable.

None of them asked whether the *ladder itself* was the limit. EXP-006's winner,
`gradient_boosting`, ran on sklearn's defaults — 100 trees, depth 3, learning
rate 0.1 — because that is what `default_specs` declares. It cleared three gates
of four and failed net Sharpe at −0.102. It is a fair question whether a
configuration that was never chosen is the best that family can do.

This experiment asks that question under control. It searches families,
hyperparameters, feature arms and targets in four staged rounds, logs every
configuration, and counts all of them against the cumulative multiple-testing
budget.

## 2. The thing that makes this dangerous, and what stops it

A search of 879 configurations will find something that looks good. That is not
a risk of the method, it is a certainty of it: the maximum of 879 draws from a
zero-skill population is large. The best of 1,035 cumulative zero-skill trials
would be expected to show |t| ≈ 3.39.

So the search pays for its own size. Three mechanisms, all fixed before the run:

1. **`prior_evaluations = 156`** — every trial from EXP-001 through EXP-006
   still counts. The register does not reset because the code changed.
2. **The `survives_search_size` gate** — the winner's |t| must exceed the
   expected maximum |t| of a zero-skill population of the same cumulative size.
   This gate did not exist in earlier studies because they did not search.
3. **The budget is printed before the run.** `python -m src.quant.train
   --experiment EXP-007 --budget overnight --performance max` prints the trial count and the
   resulting bar as part of the dry run. Choosing `overnight` over `standard`
   is choosing to raise the bar from 3.09 to 3.39 in exchange for more
   configurations.

A larger search cannot lower the bar. It is the only honest way to run one.

## 3. What is being searched

| Stage | What varies | What is held | Configurations (overnight) |
|---|---|---|---|
| 1 SCREEN | family, coarse hyperparameters | arm `C_base`, target `fwd_rank_21` | 129 |
| 2 TUNE | hyperparameters, in the families that survived | arm, target | 630 |
| 3 CONTEXT | feature arm x target, at each finalist's fixed hyperparameters | hyperparameters | 60 |
| 4 ROBUSTNESS | small perturbations around each finalist | arm, target | 60 |
| | | | **879** |

Stage 1 decides where Stage 2's budget goes. Spending it flat would tune
families the screen already showed are not competitive — 630 configurations
spread over eight families is 79 each, and the four that matter would get a
quarter of the search they deserve.

Stage 4 exists because a winner that is a single point in hyperparameter space
and a winner that sits in a region of comparable configurations are different
claims. If a finalist's ten neighbours are all far worse, the finalist is a
draw from the tail, not a setting.

## 4. Search spaces

Declared in `src/quant/study/search.py::SPACES`, one `Axis` per hyperparameter,
sampled deterministically from `(family, stage, seed)`. Re-running with the same
seed produces the same configurations, which is what makes `--resume` safe: a
resumed run continues the same search rather than starting a correlated new one.

Eight families: `gradient_boosting`, `hist_gradient_boosting`, `random_forest`,
`extra_trees`, `ridge`, `lasso`, `elastic_net`, `ols`.

`ols` carries one configuration because it has no hyperparameters. It is in the
search as the linear overfitting control, exactly as it is in `default_specs`.

## 5. Feature arms and targets

Stage 3 sweeps five arms x two targets. Arms come from
`src/quant/study/families.py::DEFAULT_ARMS`, whose membership is derived from
the feature registry's declared groups rather than hand-listed, so an arm
cannot silently drift from the family it names.

Targets are `fwd_rank_21` (primary, declared before any result was seen) and
`fwd_ret_21`.

## 6. What is NOT searched

- **Costs.** The 10 bp half-spread is not a tunable. EXP-006 established that
  its net Sharpe crosses zero between 5 and 10 bp; searching the cost
  assumption would be selecting the assumption that makes a model pass.
- **Execution lag.** Fixed at 1 rebalance period. `ExperimentDefinition`
  refuses anything lower.
- **Fold geometry.** Purge, embargo, validation length and minimum training
  length are the same as every prior study on these folds. Changing them would
  make the cumulative trial count meaningless.
- **Gate thresholds.** They are in code, they are the same as
  `ModelRegistry.CANDIDATE_THRESHOLDS`, and the search cannot reach them.
- **The holdout.** See section 11.

## 7. The gates

Eight, in `src/quant/study/heavy.py::evaluate_gates`. None move.

| Gate | Requirement |
|---|---|
| `ic_t_stat` | \|t\| ≥ 2.0 |
| `gross_sharpe` | > 0 |
| `net_sharpe` | > 0 at the declared 10 bp half-spread |
| `beats_best_baseline` | IC above the best baseline **refit on the same folds and label** |
| `not_overfit` | train − validation IC ≤ 0.15 |
| `survives_search_size` | \|t\| > the expected max \|t\| of the cumulative trial count |
| `alpha_credible` | six-factor alpha t > 0 |
| `turnover_tolerable` | ≤ 30x annualised |

The verdict is binary and unweighted. A composite score would invite tuning the
weights until the preferred configuration wins.

**Passing all eight yields DEVELOPMENT CANDIDATE, not a production model.**
Production requires the holdout, which this study does not spend.

## 8. NO PRODUCTION CANDIDATE is an expected outcome

EXP-004 found no learned model beating free factors. EXP-005 found no
additional data source adding information. EXP-006 found one model that clears
three gates of four and fails on cost. The prior that a hyperparameter search
turns that into a passing model is not high.

If nothing clears the gates, the result is recorded as NO PRODUCTION CANDIDATE
and the gates are not adjusted. That outcome is worth the compute: it converts
"we used defaults" into "we searched 879 configurations and the ceiling is
still below the cost line", which is a materially stronger statement.

## 9. Reproducibility

- Configurations are a deterministic function of `(family, stage, index, seed)`.
- Worker count does not affect results. `validation/parallel.py` sorts outcomes
  back into declared spec order before returning, because completion order
  depends on the pool size and CSCV partitions columns in insertion order —
  without the sort, the reported PBO could change with `--workers` alone. This
  was verified: a 6-worker and a 7-worker run of EXP-006 produced bit-identical
  metrics.
- Threads per worker is pinned to 1 in **every** performance policy. Multi-
  threaded BLAS can reorder reductions and change the last bits of a linear fit;
  if the thread count varied by policy, `--performance max` could produce a
  different number from `safe`.
- `select_candidate` refits each finalist and records `search_mean_ic` against
  `refit_mean_ic`. A non-zero delta means the run is not reproducible and the
  selection should not be trusted.

## 10. Crash safety

Every completed configuration is appended to
`experiments/EXP-007/checkpoints/configs.jsonl` immediately, as JSONL, append
only. A crash mid-write costs one line, which the reader skips with a warning,
rather than truncating a rewritten file.

`--resume` loads the checkpoint and skips what is recorded. Nothing is refit and
no result changes, because configurations are deterministic in the seed.

## 11. The holdout

Not read. Not armed. Not used for selection.

`build_plan` reserves the final 252 sessions before the first fold is cut.
`HoldoutFirewall.assert_clear` runs on the training frame **and** the validation
frame of every fold immediately before every fit — several hundred times over
this run. There is no environment variable that disables it;
`_env_override_is_refused` raises if `QUANT_DISABLE_HOLDOUT_FIREWALL` is set, so
the flag is worse than useless to anyone who finds it.

The training command has no code path that opens the holdout. Spending it
requires `python -m src.quant.study.holdout` under the contract in
`docs/HOLDOUT_CONTRACT.md`.

## 12. Running it

```bash
python -m src.quant.train --experiment EXP-007 --budget overnight --performance max --confirm
```

Dry run — the default, without `--confirm` — prints the machine profile, the
worker plan, the four stages, the projected wall time and the multiple-testing
cost, and fits nothing.

After an interruption:

```bash
python -m src.quant.train --experiment EXP-007 --budget overnight --performance max --confirm --resume
```

Then:

```bash
python -m scripts.quant.select_candidate --experiment EXP-007
```

Full operational detail is in [training.md](training.md).

---

# Results

Run 2026-08-31 23:02 → 2026-09-01 08:54. Search artifact
`experiments/EXP-007/search.json`; selection artifact
`artifacts/experiments/EXP-007/final_selection.json`.

## 13. Machine and compute

| | |
|---|---|
| policy | MAX |
| workers | 5 × 1 thread |
| configurations | 873 evaluated, **0 failed** |
| wall clock | 9.87 h |
| interruptions | none |

The declared upper bound was 879. The run evaluated 873 because Stage 3 skips
the finalist/context pairs already measured in Stages 1–2. The correction uses
**873**, the number actually spent, not the number budgeted.

## 14. Dataset

| | |
|---|---|
| rows | 506,374 |
| symbols | 977 |
| rebalance dates | 625 |
| window | 2014-04-01 → 2026-08-31 |
| content hash | `95473c9f6cd896d6d3be5dd563cc156a` |
| git commit | `f4f21a6e878f60eafea8ae423829450c5faf1f1c` |

## 15. Fold geometry

8 expanding walk-forward folds, 252-session validation, 756-session minimum
training, 21-session label horizon purged, 5-session embargo. Holdout
2025-08-26 → 2026-08-26, **reserved before the first fold was cut and never
read**.

## 16–18. Stages

| stage | configurations | worker-hours |
|---|---|---|
| 1 screen | 129 | 4.4 |
| 2 tune | 630 | ~33 |
| 3 context | 54 | ~8 |
| 4 robustness | 60 | ~4 |

Families advanced from the screen: `random_forest`,
`hist_gradient_boosting`, `gradient_boosting`, `extra_trees`. The four linear
families (`ridge`, `lasso`, `elastic_net`, `ols`) all plateaued at IC ≈ +0.0053,
t ≈ +0.36 and did not advance.

**584 of 813 configurations recorded by the end of Stage 3 exceeded the
overfitting gap of 0.15.** The diagnostic did most of the work in this study.

## 19. Finalists

Six, deduplicated by (family, arm, target). All on `C_base` / `fwd_rank_21`.

| family | IC | t | gross Sharpe | net Sharpe | turnover | alpha t | gap |
|---|---|---|---|---|---|---|---|
| hist_gradient_boosting | +0.0332 | +2.81 | +0.498 | **+0.111** | 18.5× | +0.48 | 0.0909 |
| gradient_boosting | +0.0364 | +3.24 | +0.482 | +0.077 | 18.8× | +0.33 | 0.0949 |
| random_forest | +0.0457 | +3.47 | +0.420 | +0.064 | 17.7× | +0.32 | 0.0956 |
| random_forest | +0.0322 | +2.55 | +0.270 | +0.047 | 13.0× | +0.25 | 0.1143 |
| random_forest | +0.0396 | +3.34 | +0.253 | −0.113 | 17.9× | −0.02 | 0.0863 |
| extra_trees | +0.0312 | +2.08 | +0.013 | −0.407 | 16.5× | −1.05 | 0.1043 |

**Read the scatter.** Configurations with nearly identical IC produce net
Sharpes from −0.113 to +0.111. Rank ordering by IC and by net Sharpe disagree.
That instability is the finding, and section 23 measures it.

## 20. Selected configuration

`hist_gradient_boosting`, config `9d1651c56782`, ranked by **net Sharpe at the
declared 10 bp half-spread** — the criterion written into
`select_candidate.py` before the run.

```
max_iter            65
learning_rate       0.029748760596688115
max_depth           3
min_samples_leaf    104
l2_regularization   0.0003893482481762855
```

| metric | value |
|---|---|
| validation IC | +0.03324 |
| IC t-statistic | +2.810 |
| IC IR | 0.2160 |
| train IC | +0.12415 |
| train − validation gap | 0.09091 |
| folds with positive IC | 7 of 8 (87.5%) |
| features | 27 (`C_base`) |
| gross Sharpe | +0.4983 |
| **net Sharpe** | **+0.1106** |
| turnover | 18.47× annualised |
| six-factor alpha t | +0.480 |

**This is the first configuration in the project with a positive net Sharpe.**
EXP-006 posted −0.102. That is a real improvement and it is not sufficient; see
section 23.

## 21. Robustness

Sixty neighbour configurations around the finalists. Reported inside
`search.json`; the neighbourhoods did not overturn the finalist ordering, but
see the PBO result — neighbourhood stability and *selection* stability are
different things and only the second one matters for promotion.

## 22. Economics and cost sensitivity

| half-spread | gross Sharpe | net Sharpe | net CAGR | cost / gross |
|---|---|---|---|---|
| 1 bp | +0.498 | +0.325 | +3.97% | 34.7% |
| 3 bp | +0.498 | +0.277 | +3.20% | 44.3% |
| 5 bp | +0.498 | +0.230 | +2.44% | 53.9% |
| **10 bp (declared)** | +0.498 | **+0.111** | +0.57% | **77.8%** |
| 20 bp | +0.498 | −0.127 | −3.08% | 125.5% |

At the declared assumption **costs consume 77.8% of gross return**, and the
result turns negative between 10 and 20 bp. The 10 bp figure was not revisited
after seeing this. Sensitivity analysis is reported; the declared assumption is
unchanged.

## 23. Significance — the section that decides this study

Three independent statistics, all pointing the same way.

**Search-size threshold.** 156 prior + 873 new = **1,029 cumulative trials**.
The expected maximum |t| of 1,029 zero-skill configurations is **3.38**;
Bonferroni at 5% is 4.06. The selected configuration posts **+2.810**.

**Deflated Sharpe Ratio** (Bailey & López de Prado), computed against the
dispersion of the trial Sharpes rather than an iid null, so trial correlation is
already accounted for:

| | |
|---|---|
| observed Sharpe | 0.1106 |
| expected max Sharpe under the null | **0.6439** |
| deflated probability | **0.0608** (needs > 0.95) |
| trials | 1,029 |
| observations | 403 |
| skew / excess kurtosis | 3.61 / 43.28 |

The best of 1,029 zero-skill configurations would be expected to show a Sharpe
of 0.64. We observed 0.11 — **0.17× the search's own noise ceiling.**

**Probability of Backtest Overfitting** via CSCV, 70 splits over 8 blocks:

> **PBO = 0.929.** The in-sample winner landed in the bottom half out-of-sample
> in 93% of splits.

0.5 means selection carries no information. **0.93 means selection is worse
than a coin flip** — in this search, picking the in-sample best is actively
anti-predictive out-of-sample.

**Minimum Track Record Length.** 10,545 periods required for 95% confidence
that the Sharpe exceeds zero. We have **403**. Twenty-six times short.

## 24. Regimes

Reported in `search.json`. Not quoted here: with the study failing three
significance tests, a regime breakdown would be picking through subsamples of a
result that is not established at the top level.

## 25. Reproduction check

All six finalists were refit from scratch and compared to the search record:

| finalist | search IC | refit IC | delta |
|---|---|---|---|
| all six | — | — | **0.0 exactly** |

Bit-identical. The run is reproducible.

## 26. Gates

Evaluated under the **ten-gate standard** (the original eight plus
`deflated_sharpe` and `selection_carries_information`, registered 2026-09-01 —
see section 27).

| gate | required | observed | |
|---|---|---|---|
| ic_t_stat | ≥ 2.0 | +2.810 | PASS |
| gross_sharpe | > 0 | +0.498 | PASS |
| net_sharpe | > 0 at 10 bp | +0.111 | PASS |
| beats_best_baseline | > +0.02087 | +0.03324 | PASS |
| not_overfit | ≤ 0.15 | 0.0909 | PASS |
| **survives_search_size** | **> 3.38** | **+2.810** | **FAIL** |
| alpha_credible | > 0 | +0.480 | PASS |
| turnover_tolerable | ≤ 30× | 18.47× | PASS |
| **deflated_sharpe** | **> 0.95** | **0.0608** | **FAIL** |
| **selection_carries_information** | **PBO ≤ 0.20** | **0.929** | **FAIL** |

## 27. Verdict

# NO PRODUCTION CANDIDATE

**Seven of ten gates pass. Three fail, and they are the three that test whether
the result is a selection artifact.**

### The disclosure that matters

One finalist — `random_forest`, IC +0.0457, **t +3.47** — clears
`survives_search_size`, and under the original eight-gate standard it passes
**every gate**. It is not the selected configuration only because
`select_candidate.py` ranks by net Sharpe, a criterion fixed in code before the
run.

**We did not switch the ranking criterion.** Changing how a winner is chosen
after observing that a different rule would produce a pass is specification
search, and it is the precise failure that pre-registration exists to prevent.

It would also not have helped. That configuration posts a deflated-Sharpe
probability of **0.0485** and shares the **PBO of 0.929**. It passes eight gates
and fails the two statistics that actually test selection bias.

That is why the standard was strengthened rather than the ranking changed. The
eight-gate set had a real hole: `survives_search_size` compares an *IC*
t-statistic against a threshold derived for *Sharpe* selection, and nothing
consulted the deflated Sharpe or the PBO this project already computes. Both are
now gates. The change makes promotion strictly harder and rejects the model in
front of it — the only kind of threshold change admissible after seeing results.

### What this study establishes

- **PROVEN.** The run is reproducible: six finalists refit bit-identically.
- **PROVEN.** Hyperparameter search improves the *point estimate*: net Sharpe
  moved from −0.102 (EXP-006 defaults) to +0.111. Turnover fell from 20.1× to
  18.5×.
- **PROVEN.** That improvement does not survive correction for the size of the
  search that produced it. Three independent tests agree.
- **SUPPORTED.** Tree families dominate linear families on this panel by roughly
  an order of magnitude in IC (+0.033 to +0.046 versus +0.005).
- **SUPPORTED.** The result is cost-fragile: 77.8% of gross is consumed at the
  declared 10 bp, and the sign flips before 20 bp.
- **REJECTED.** The hypothesis that the fixed ladder was the binding constraint.
  It was not. Searching 873 configurations raised the bar faster than it raised
  the result.
- **NOT TESTED.** Everything requiring the holdout. It was not opened.

### The bottleneck, quantified

| | |
|---|---|
| effective independent blocks | 403 periods × 5 sessions ÷ 21-session label ≈ **96** |
| trials spent on them | **1,029** |
| **trials per independent block** | **10.7** |

The search is roughly eleven times larger than the information content of the
sample it was run on. This is not a model-capacity problem and **more search
makes it strictly worse**: every additional configuration raises the threshold
and, as the PBO shows, degrades selection reliability.

**Do not run another hyperparameter search on this panel.** The next question
is in `docs/EXP-008.md`.

## 28. Holdout

**SEALED.** `touched: false` in both artifacts. The contract is not armed.
Nothing in this study read a holdout-dated row; the firewall asserted on both
frames of every fold before every one of the 873 fits.
