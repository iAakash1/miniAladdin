# Feature Library

> Generated by `python -m scripts.quant.render_feature_docs`. Do not edit by hand —
> it is rendered from `src/quant/features/registry.py`, so it cannot disagree with
> the code that builds the panel.

Every feature declares a lookback, a direction hypothesis recorded **before**
testing, and its point-in-time status. A feature that is not registered cannot
enter an experiment.

## Reading the table

* **Lookback** — sessions of history the value depends on. The dataset builder
  refuses a symbol block shorter than the longest lookback.
* **Direction** — the sign of the *hypothesised* relationship, written down in
  advance so a factor that works with the opposite sign is visibly a surprise
  rather than retroactively "what we expected". `descriptive` claims none.
* **PIT** — whether the value is knowable on its own date. Everything here is
  point-in-time safe; sources that are not are refused upstream, in the catalog.

Features are consumed as **cross-sectional ranks** within the point-in-time
universe (suffix `_xs`), except macro features which are used raw. Ranking is
fitted per date, which is why it cannot leak across dates.


**57 registered features** across 7 groups.


## fundamental (22)

Two very different sources share this group and must not be read alike.

`est_*` come from `eps_estimate` / `sales_estimate`, which are keyed by
OBSERVATION date — they need no publication gate and carry no restatement
risk. `fund_*` come from the statement tables, which are keyed by fiscal
PERIOD END; they reach the panel only behind an `earnings_calendar`
announcement join, and they carry **UNQUANTIFIED restatement risk** because
the source holds one row per period with no vintage column.


| feature | description | formula | lookback | direction | PIT |
|---|---|---|---|---|---|
| `earn_days_since` | Approximate trading sessions since the latest available print. | `(observation date - availability date) in calendar days x 5/7` | 1 | negative | yes |
| `earn_sue` | Standardised unexpected earnings: surprise over its own trailing dispersion. | `(reported - estimate) / std(prior surprises, expanding, >= 4 observations)` | 1 | positive | yes |
| `earn_surprise_pct` | Latest EPS surprise as a fraction of \|estimate\|, available from the print. | `(reported - estimate) / \|estimate\|, attached from the announcement's availability date` | 1 | positive | yes |
| `earn_surprise_sign` | Sign of the latest available surprise (-1, 0, +1). | `sign(surprise_pct)` | 1 | positive | yes |
| `est_eps_coverage` | Number of analysts contributing an FY1 EPS estimate. | `count` | 0 | descriptive | yes |
| `est_eps_coverage_chg_13w` | Change in analyst count over 13 weekly vintages. | `count_t - count_(t-13w)` | 65 | positive | yes |
| `est_eps_dispersion` | Spread of FY1 EPS estimates, scaled by consensus. | `(high - low) / \|consensus\|` | 0 | negative | yes |
| `est_eps_growth_expected` | FY1 consensus against the year-ago realised figure. | `(consensus - year_ago) / \|year_ago\|` | 0 | descriptive | yes |
| `est_eps_rev_13w` | Change in FY1 EPS consensus over 13 weekly vintages. | `(consensus_t - consensus_(t-13w)) / \|consensus_(t-13w)\|, only where period_end_date is unchanged` | 65 | positive | yes |
| `est_eps_rev_4w` | Change in FY1 EPS consensus over 4 weekly vintages. | `(consensus_t - consensus_(t-4w)) / \|consensus_(t-4w)\|, only where period_end_date is unchanged` | 20 | positive | yes |
| `est_sales_rev_13w` | Change in FY1 revenue consensus over 13 weekly vintages. | `(consensus_t - consensus_(t-13w)) / \|consensus_(t-13w)\|, only where period_end_date is unchanged` | 65 | positive | yes |
| `est_sales_rev_4w` | Change in FY1 revenue consensus over 4 weekly vintages. | `(consensus_t - consensus_(t-4w)) / \|consensus_(t-4w)\|, only where period_end_date is unchanged` | 20 | positive | yes |
| `fund_accruals` | Earnings less operating cash flow, over assets. | `(net_income - net_cash_from_operating_activities) / total_assets` | 0 | negative | yes |
| `fund_asset_growth_yoy` | Year-over-year change in total assets. | `(total_assets_t - total_assets_(t-4q)) / total_assets_(t-4q)` | 252 | negative | yes |
| `fund_current_ratio` | Current assets over current liabilities. | `total_current_assets / total_current_liabilities` | 0 | positive | yes |
| `fund_debt_to_equity` | Total liabilities over total equity. | `total_liabilities / total_equity` | 0 | negative | yes |
| `fund_gross_margin` | Gross profit over sales. | `gross_profit / sales` | 0 | positive | yes |
| `fund_net_issuance_yoy` | Year-over-year change in shares outstanding. | `(shares_t - shares_(t-4q)) / shares_(t-4q)` | 252 | negative | yes |
| `fund_operating_margin` | Pretax income over sales. | `pretax_income / sales` | 0 | positive | yes |
| `fund_roa` | Net income over total assets. | `net_income / total_assets` | 0 | positive | yes |
| `fund_roe` | Net income over total equity. | `net_income / total_equity` | 0 | positive | yes |
| `fund_sales_growth_yoy` | Year-over-year change in sales. | `(sales_t - sales_(t-4q)) / sales_(t-4q)` | 252 | descriptive | yes |

<details><summary>Citations</summary>

* `earn_sue` — Foster, Olsen & Shevlin (1984)
* `earn_surprise_pct` — Ball & Brown (1968); Bernard & Thomas (1989)
* `est_eps_dispersion` — Diether, Malloy & Scherbina (2002)
* `est_eps_rev_13w` — Chan, Jegadeesh & Lakonishok (1996); Stickel (1991)
* `est_eps_rev_4w` — Chan, Jegadeesh & Lakonishok (1996); Stickel (1991)
* `fund_accruals` — Sloan (1996)
* `fund_asset_growth_yoy` — Cooper, Gulen & Schill (2008)
* `fund_gross_margin` — Novy-Marx (2013)
* `fund_net_issuance_yoy` — Pontiff & Woodgate (2008)

</details>

> **10 features in this group carry UNQUANTIFIED restatement risk.** A later correction overwrites the original figure in the source, and the magnitude of that effect cannot be measured from the source alone.


## macro (11)

| feature | description | formula | lookback | direction | PIT |
|---|---|---|---|---|---|
| `market_drawdown` | Market drawdown from its trailing-year peak, positive. | `1 - index / max(index over 252)` | 252 | descriptive | yes |
| `market_mom_21` | Equal-weighted universe return over 21 sessions. | `compounded cross-sectional mean return over 21 sessions` | 21 | descriptive | yes |
| `market_mom_252` | Equal-weighted universe return over 252 sessions. | `compounded cross-sectional mean return over 252 sessions` | 252 | descriptive | yes |
| `market_vol_21` | Annualised volatility of the equal-weighted universe return, 21 sessions. | `std(market return, 21) * sqrt(252)` | 21 | descriptive | yes |
| `market_vol_63` | Annualised market volatility over 63 sessions. | `std(market return, 63) * sqrt(252)` | 63 | descriptive | yes |
| `market_vol_percentile` | Rank of current market volatility in its trailing 2-year distribution. | `rolling(504).rank(pct=True) of market_vol_21` | 504 | descriptive | yes |
| `rates_change_63` | Quarterly change in the 10-year yield. | `10_year(T) - 10_year(T-63), lagged one session` | 64 | negative | yes |
| `rates_curvature` | 2*2y - 3m - 10y, prior session. | `(2*2_year - 3_month - 10_year) / 100, lagged one session` | 1 | descriptive | yes |
| `rates_level` | 10-year US Treasury par yield, prior session. | `10_year / 100, lagged one session` | 1 | descriptive | yes |
| `rates_short` | 3-month Treasury bill yield, prior session. | `3_month / 100, lagged one session` | 1 | descriptive | yes |
| `rates_slope` | 10-year minus 2-year yield, prior session. | `(10_year - 2_year) / 100, lagged one session` | 1 | descriptive | yes |

<details><summary>Citations</summary>

* `rates_curvature` — Litterman & Scheinkman (1991)

</details>

## options (8)

Derived from `volatility_history` and aggregated `option_chain`. Note what
the schema does **not** contain: there is no open interest and no traded
volume, so put/call volume imbalance, open-interest imbalance, dealer gamma
exposure and unusual-activity measures are **not computable** from this
source and are not implemented. Claiming them would require inventing them.


| feature | description | formula | lookback | direction | PIT |
|---|---|---|---|---|---|
| `opt_expirations` | Distinct expirations listed on the snapshot date. | `count(distinct expiration)` | 1 | descriptive | yes |
| `opt_iv` | Implied volatility of the underlying, latest snapshot at or before the date. | `iv_current from volatility_history` | 1 | negative | yes |
| `opt_iv_change_21` | Change in implied volatility over roughly one month. | `iv_current / iv_month_ago - 1` | 1 | two_sided | yes |
| `opt_iv_minus_hv` | Implied over realised volatility, as a ratio minus one. | `iv_current / hv_current - 1` | 1 | two_sided | yes |
| `opt_iv_rank` | Position of current IV within its trailing 52-week range. | `(iv_current - iv_year_low) / (iv_year_high - iv_year_low)` | 1 | two_sided | yes |
| `opt_rel_spread` | Mean relative bid-ask spread across the chain. | `mean (ask - bid) / mid over quotes with both sides positive` | 1 | negative | yes |
| `opt_skew_25d` | 25-delta put IV minus 25-delta call IV, normalised by ATM IV. | `(put_25_iv - call_25_iv) / atm_iv` | 1 | two_sided | yes |
| `opt_term_slope` | Far-expiry ATM IV over near-expiry ATM IV, minus one. | `atm_iv(>45d) / atm_iv(<=45d) - 1` | 1 | two_sided | yes |

## price (5)

| feature | description | formula | lookback | direction | PIT |
|---|---|---|---|---|---|
| `acceleration` | Trailing-month return minus the quarter-return ending a month ago. | `r_21(T) - r_63(T - 21)` | 84 | two_sided | yes |
| `mom_21` | Compounded total return over the trailing 21 sessions. | `prod(1 + r_t) over t in (T-20, T] - 1` | 21 | two_sided | yes |
| `mom_252_21` | 12-1 momentum: trailing year excluding the most recent month. | `(1 + r_252) / (1 + r_21) - 1` | 252 | positive | yes |
| `mom_63` | Compounded total return over the trailing 63 sessions. | `prod(1 + r_t) over t in (T-62, T] - 1` | 63 | positive | yes |
| `reversal_5` | Negated trailing 5-session return. | `-(prod(1 + r_t) over the last 5 sessions - 1)` | 5 | positive | yes |

<details><summary>Citations</summary>

* `mom_252_21` — Jegadeesh & Titman (1993)
* `reversal_5` — Lehmann (1990); Jegadeesh (1990)

</details>

## structure (3)

| feature | description | formula | lookback | direction | PIT |
|---|---|---|---|---|---|
| `dist_52w_high` | Fractional distance below the trailing-year high (<= 0). | `index_T / max(index over 252) - 1` | 252 | positive | yes |
| `ma_gap` | Log-index 21-session mean minus 126-session mean. | `mean(log_index, 21) - mean(log_index, 126)` | 126 | positive | yes |
| `trend_strength_63` | 63-session return divided by 63-session annualised volatility. | `mom_63 / vol_63` | 63 | positive | yes |

<details><summary>Citations</summary>

* `dist_52w_high` — George & Hwang (2004)

</details>

## volatility (5)

| feature | description | formula | lookback | direction | PIT |
|---|---|---|---|---|---|
| `downside_vol_63` | Downside deviation over 63 sessions, annualised. | `sqrt(mean(min(r, 0)^2, 63)) * sqrt(252)` | 63 | negative | yes |
| `max_drawdown_252` | Deepest peak-to-trough decline in the trailing year, positive. | `max over the window of (peak - value) / peak` | 252 | negative | yes |
| `vol_21` | Annualised realised volatility over 21 sessions. | `std(r, 21) * sqrt(252)` | 21 | negative | yes |
| `vol_63` | Annualised realised volatility over 63 sessions. | `std(r, 63) * sqrt(252)` | 63 | negative | yes |
| `vol_ratio` | 21-session volatility divided by 63-session volatility. | `vol_21 / vol_63` | 63 | two_sided | yes |

## volume (3)

| feature | description | formula | lookback | direction | PIT |
|---|---|---|---|---|---|
| `amihud_21` | Log Amihud illiquidity over 21 sessions. | `log(mean(\|r\| / dollar_volume * 1e6, 21))` | 21 | positive | yes |
| `log_dollar_volume_21` | Log of mean daily dollar volume over 21 sessions. | `log(mean(close * volume, 21))` | 21 | descriptive | yes |
| `volume_shock` | Dollar volume z-scored against its own trailing 63 sessions. | `(dv - mean(dv, 63)) / std(dv, 63)` | 63 | two_sided | yes |

<details><summary>Citations</summary>

* `amihud_21` — Amihud (2002)

</details>
